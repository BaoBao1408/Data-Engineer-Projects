# ── Core pipeline ────────────────────────────────────────────────
prefect>=2.16.0,<3.0.0          # Workflow orchestration (scheduler + UI)
pandas>=2.1.0,<3.0.0            # DataFrame transformations
numpy>=1.26.0,<2.0.0            # Numeric operations (pandas dependency)
python-dotenv>=1.0.0            # Load .env file for settings

# ── AWS SDK + Data Tools ─────────────────────────────────────────
boto3>=1.34.0,<2.0.0            # AWS SDK — S3, Glue, Athena, SNS, IAM
awswrangler>=3.5.0,<4.0.0       # Pandas-native AWS SDK: S3 Parquet I/O + Athena queries
                                # (wraps boto3; boto3 is included as a dependency automatically)

# ── Parquet / Columnar Storage ───────────────────────────────────
pyarrow>=14.0.0,<16.0.0         # Parquet read/write engine (used by awswrangler)
fastparquet>=2023.10.0          # Alternative Parquet engine (optional fallback)

# ── Local dev / Testing ──────────────────────────────────────────
duckdb>=0.10.0,<1.0.0           # Local mode warehouse (no AWS needed during dev)

# ── Code quality ─────────────────────────────────────────────────
pytest>=7.4.0
pytest-mock>=3.12.0             # Mock boto3/S3 calls in unit tests
moto[s3,glue,athena]>=4.2.0    # AWS service mocks for tests (no real AWS needed)
ruff>=0.1.0                     # Linter + formatter
mypy>=1.7.0                     # Type checking

# ── Monitoring / Logging ─────────────────────────────────────────
structlog>=23.2.0               # Structured JSON logging (production)