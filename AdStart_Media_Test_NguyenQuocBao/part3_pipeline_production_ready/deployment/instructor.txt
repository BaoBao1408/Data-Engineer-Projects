docker build -f deployment/Dockerfile -t adstart-pipeline .

docker run --rm `
-v ${PWD}/data:/app/data `
-v ${PWD}/logs:/app/logs `
adstart-pipeline --date 2026-01-15

--Build Image
docker compose build

--Run Pipeline
docker compose up

test:
	pytest tests/ -v

--if we run success:
data/warehouse/warehouse.duckdb

--verify container
docker ps

--viewlog:
docker logs adstart_pipeline

healthcheck:
  test: ["CMD", "python", "-c", "import duckdb"]