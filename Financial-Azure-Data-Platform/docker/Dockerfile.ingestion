FROM python:3.11-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc g++ curl poppler-utils tesseract-ocr \
    unixodbc-dev libpq-dev \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY requirements.txt requirements-ingestion.txt ./
RUN pip install --no-cache-dir -r requirements.txt -r requirements-ingestion.txt

COPY src/ ./src/
COPY config/ ./config/

RUN adduser --disabled-password --gecos '' appuser && \
    chown -R appuser:appuser /app
USER appuser

CMD ["python", "-m", "src.ingestion.main"]
