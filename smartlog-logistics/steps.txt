1. Mở Docker Desktop → đợi xanh
2. Cài Node.js → restart VSCode
3. docker-compose up -d zookeeper kafka   ← chỉ cần Kafka thôi, DB dùng local pgAdmin
4. cd C:\Projects\Data-Engineer-Projects\smartlog-logistics\OrderService
dotnet run
5. cd C:\Projects\Data-Engineer-Projects\smartlog-logistics\TrackingService
dotnet run
6. cd C:\Projects\Data-Engineer-Projects\smartlog-logistics\react-frontend
npm install
npm run dev

Bước tiếp theo — Test flow end-to-end
1. Mở Swagger test API
Mở browser:

http://localhost:5001/swagger → Order API
http://localhost:5002/swagger → Tracking API

2. Test flow Kafka thật sự

Bước 1 — Login lấy token tại Swagger OrderService:
jsonPOST /api/auth/login
{ "username": "admin", "password": "smartlog123" }
Copy token trả về → bấm Authorize trên Swagger → paste vào.

docker exec smartlog-kafka kafka-topics --create `
  --bootstrap-server localhost:9092 `
  --topic order.created `
  --partitions 3 `
  --replication-factor 1

docker exec smartlog-kafka kafka-topics --create `
  --bootstrap-server localhost:9092 `
  --topic order.status.updated `
  --partitions 3 `
  --replication-factor 1

Verify:
powershelldocker exec smartlog-kafka kafka-topics --list --bootstrap-server localhost:9092
Phải thấy:
order.created
order.status.updated

Bước 2 — Tạo đơn hàng:
jsonPOST /api/orders
{
  "customerName": "Nguyen Van A",
  "customerPhone": "0901234567",
  "originAddress": "123 Nguyen Hue Q1 HCM",
  "destinationAddress": "456 Le Loi Q3 HCM",
  "items": [
    { "productName": "Laptop", "quantity": 1, "weight": 2.5 }
  ]
}
Bước 3 — Mở Kafka UI http://localhost:8090 → xem topic order.created có message chưa
Bước 4 — Check TrackingService tự tạo record:
GET http://localhost:5002/api/tracking/{orderCode}
Bước 5 — Update status → xem Kafka event mới:
jsonPATCH /api/orders/{id}/status
{ "status": "Confirmed" }
3. Test React Frontend
Mở http://localhost:3000:

Login: admin / smartlog123
Xem danh sách orders
Tạo đơn mới qua form
Tra cứu tracking bằng order code